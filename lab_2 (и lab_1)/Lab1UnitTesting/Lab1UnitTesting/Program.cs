using System;

namespace Lab1UnitTesting
{
    class Program
    {
        static void Main(string[] args)
        {
            //StringFormatter sf = new StringFormatter();
            //string test = sf.SaveString("insert update");
            //Console.WriteLine(test);
            //ArrayProcessor array = new ArrayProcessor();
            //int[] temp = { -3, -10, 9, 9, 8, -3, -10, 4, 5, 1 };
            //int[] test = array.SortAndFilter(temp);
            //for (int i = 0; i < test.Length; i++)
            //{
            //    Console.Write(test[i] + "  ");
            //}

            //SqlQueryPreparator sql = new SqlQueryPreparator();
            //String[] testQueries = { "select user from customs where name='test'", "delete tables", " " };
            //sql.PrepareQueries(testQueries);

            //LynearEquationsSystem les = new LynearEquationsSystem();
            //double[,] test = new double[3, 4]{ { 1, 2, 3, 1 }, { 3, 2, 1, 2 }, { 1, 3, 2, 3 } };
            //les.SetCoefs(test);
            //Console.WriteLine(les.Det());
            //int j = 1;
            //for (int i = 0; i < 3; i++)
            //{
            //    Console.WriteLine("x"+ j++ +" = "+les.Solve()[i]);
            //}

            //FileServises fs = new FileServises();
            //fs.RemoveTemporaryFiles("D:\\VisualStudio2019\\ToRead.txt");
        }
    }
}
